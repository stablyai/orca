package expo.modules.orcamobilewebshell

object BuildConfig { const val BROWSER_FIXTURE = true }
class BrowserGuestActivity
class BrowserGuestService
// Route parsing has its own Android unit tests; this harness isolates ownership.
object BrowserGuestRoute { fun parse(raw: String) {} }
