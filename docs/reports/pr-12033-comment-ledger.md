# PR #12033 comment ledger

Snapshot: `stablyai/orca#12033` at head `97dfedfdce5107bf6853689fe2bc4010639bff1b`, inspected on 2026-08-02 through the issue-comment, review, and review-comment APIs. This ledger records repository disposition only; no GitHub comments, reviews, or thread states were mutated.

## Inline review threads

| Thread ID                | Classification | Evidence                                                                                                                                                                                                                                                                            |
| ------------------------ | -------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `discussion_r3697405006` | valid+fixed    | `conversation-wake-migration.test.ts` clears both `db` and `root` after cleanup.                                                                                                                                                                                                    |
| `discussion_r3697405010` | valid+fixed    | `is disabled by default and honors the runtime kill switch` now separately exercises disabled feature, closed kill switch, and enabled/open behavior.                                                                                                                               |
| `discussion_r3697405015` | valid+fixed    | The two-database claim test now disposes both services, closes both databases, and removes its directory in `finally`.                                                                                                                                                              |
| `discussion_r3697411431` | valid+fixed    | `recordConversationWakeProvenanceIfCurrentInTransaction` skips wake provenance for an older still-authorized legacy/federated dispatch without rolling back its message; `commits stale legacy mail without recording wake provenance for a newer dispatch` proves the legacy case. |
| `discussion_r3697411434` | valid+fixed    | The schema ledger documents v24 and the `current === 23` migration has a one-line invariant comment in `db.ts`.                                                                                                                                                                     |
| `discussion_r3697411438` | valid+fixed    | `onOrchestrationMessageCommitted` initializes the DB first and `ensureOrchestrationConversationWake` rejects a missing DB instead of casting `null`.                                                                                                                                |
| `discussion_r3697411443` | valid+fixed    | `conversation-wake-identifiers.ts` is now the single source for the bounded ID predicate, 512-character limit, runtime message tuple, and derived message type. The SQL guard interpolates the same constant.                                                                       |
| `discussion_r3699616370` | valid+fixed    | The smoke script uses one `hasExited` predicate covering `exitCode` and `signalCode` in readiness, stop, and wait paths.                                                                                                                                                            |
| `discussion_r3699616372` | already fixed  | Controlled CLI creation intentionally sends `presentation: 'focused'` with `controlledCoordinator: true`; existing terminal handler tests assert that exact payload. This path is a coordinator UI contract, not ordinary background terminal creation.                             |
| `discussion_r3699616374` | valid+fixed    | Server startup captures the socket device/inode before hardening and passes it into rollback. `removes the owned socket when post-readiness hardening fails` proves the orphan is removed.                                                                                          |
| `discussion_r3699616375` | valid+fixed    | Production launch readiness, stop, and exit-wait paths use `hasChildExited`; tests cover both readiness and stop after signal termination.                                                                                                                                          |
| `discussion_r3699616376` | valid+fixed    | Manager-suite cleanup uses `Promise.allSettled`, always removes roots/restores mocks, then reports aggregated disposal failures.                                                                                                                                                    |
| `discussion_r3699616388` | valid+fixed    | The Unix-only manager suite is skipped on Windows, and all temporary roots use `tmpdir()` plus `path.join`.                                                                                                                                                                         |
| `discussion_r3699616391` | valid+fixed    | The fake-timer stop test restores real timers in `finally`.                                                                                                                                                                                                                         |
| `discussion_r3699616393` | valid+fixed    | The Unix client preserves JSON-RPC `code` and `data`; manager missing-thread classification uses Codex 0.145's observed `-32600` code with the prior message fallback. Tests use a non-English-shaped message to prove code-based classification.                                   |
| `discussion_r3699616395` | valid+fixed    | `CodexControlledSessionRegistry` serializes in-flight launch attempts by conversation ID. `serializes concurrent launches for one conversation` proves one process and created/reused results.                                                                                      |
| `discussion_r3699616400` | valid+fixed    | Client identity rejection closes the connected client; terminal surface/identity rejection closes the created terminal before server rollback. Both terminal rejection paths have regression coverage.                                                                              |
| `discussion_r3699616402` | valid+fixed    | Disposal marks a session missing before fallible cleanup, so a session with a closed client cannot be reused. The rollback-cleanup test now attempts relaunch and verifies fail-closed retry-only state.                                                                            |
| `discussion_r3699616405` | valid+fixed    | State loading catches malformed JSON, validates `turns` as a plain object, and validates every turn record. Three corrupt-state shapes have regression coverage.                                                                                                                    |
| `discussion_r3699616408` | already fixed  | Controlled launch is explicitly rejected unless `process.platform` is Darwin or Linux before any state store is constructed; the Unix-dependent suite now also skips Windows. No Windows prompt file is created by the supported path.                                              |
| `discussion_r3699616411` | valid+fixed    | Connect timeout, socket error, and initialize rejection all terminate the WebSocket, with a temporary error observer preventing `ws` from emitting an unhandled pre-open termination error. Dedicated client tests cover all three stages.                                          |
| `discussion_r3699616418` | valid+fixed    | WebSocket notification listeners and manager terminal observers are isolated per listener; tests prove later listeners still receive the event.                                                                                                                                     |
| `discussion_r3699616421` | valid+fixed    | Controlled replay retention is set after a successful launch, or on an explicit unknown-outcome error only. Tests prove ordinary pre-spawn failures retry and unknown outcomes remain fenced.                                                                                       |

## Issue comments and reviews

| Comment or review ID                                          | Classification | Evidence                                                                                                                                                                                                                              |
| ------------------------------------------------------------- | -------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `issuecomment-5154661904` / `IC_kwDORpCz1s8AAAABMz3mEA`       | not-applicable | Automated walkthrough and PR-template warnings contain no additional code defect. Screenshot/template sections are PR-authoring metadata, outside this repository patch.                                                              |
| `issuecomment-5154670260` / `IC_kwDORpCz1s8AAAABMz4GtA`       | valid+fixed    | Greptile's two concrete runtime concerns are covered: failed bind cleanup preserves the binding error, and test-only DB replacement no longer drops a rejection silently. Live shutdown already awaits service disposal.              |
| `pullrequestreview-4836654977` / `PRR_kwDORpCz1s8AAAABIEl_gQ` | valid+fixed    | Its outside-diff DB initialization finding and all correctness-bearing wake notes are addressed below.                                                                                                                                |
| `pullrequestreview-4836662510` / `PRR_kwDORpCz1s8AAAABIEmc7g` | valid+fixed    | Its three inline findings are `discussion_r3697411431`, `discussion_r3697411434`, and `discussion_r3697411443`, all fixed above.                                                                                                      |
| `pullrequestreview-4839071876` / `PRR_kwDORpCz1s8AAAABIG5ghA` | valid+fixed    | All 16 inline findings are classified above; correctness-bearing review-body notes are addressed below.                                                                                                                               |
| `pullrequestreview-4839095505` / `PRR_kwDORpCz1s8AAAABIG680Q` | already fixed  | The delayed SSH attempt is fenced by `connectGeneration`/`isCurrentConnectAttempt`, explicit connection clears a pending reconnect timer, and existing delayed-connect/reconnect tests cover those cases. No wake change is required. |

## Review-body note fingerprints

The review API embeds these notes in review bodies rather than assigning separate GitHub discussion IDs.

| Fingerprint                | Classification   | Evidence                                                                                                                                                                                                                           |
| -------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `2ea6655bf9c085f10afebcb6` | valid+fixed      | DB initialization/null guard fixed in `orca-runtime.ts`.                                                                                                                                                                           |
| `d949dfdf204f3dbbacdd8530` | valid+fixed      | Message tuple and derived type centralized in `conversation-wake-identifiers.ts`.                                                                                                                                                  |
| `13f5781e8d5e3c5a0a53a29e` | valid+fixed      | v24 added to the schema ledger.                                                                                                                                                                                                    |
| `5c83a67a791746b5f04d5c09` | valid+fixed      | Equality-only v23 migration invariant documented.                                                                                                                                                                                  |
| `be2fa2f32158e9f4ef0a86b0` | valid+fixed      | Job catch path reads the current row once.                                                                                                                                                                                         |
| `cc0b8a86d81f7ccfe452f256` | valid+fixed      | TypeScript and SQL provider-turn bounds share one constant.                                                                                                                                                                        |
| `c7b14c86faa13228ddb3cc5f` | not-applicable   | Optional extraction of trusted-lineage resolution is a stylistic refactor, not a behavior or safety defect. Existing forgery tests remain focused at the transactional boundary.                                                   |
| `617f543e477ef65f862ee3f8` | already fixed    | Orchestration RPC tests already cover suppressed/rejected send behavior and heartbeat exclusion; wake provenance tests cover eligible committed messages.                                                                          |
| `5eac49f0cd22e74d3d543da8` | valid+fixed      | Constructor unwinds prior provider subscriptions if a later provider returns an invalid subscription.                                                                                                                              |
| `b954241122da4daf52ce80a8` | valid+fixed      | Reconcile enqueues all processable jobs concurrently while `enqueueConversation` retains per-conversation serialization.                                                                                                           |
| `a66285133e57f2420d133cbd` | valid+fixed      | Terminal callbacks no longer run a global backfill scan; startup reconcile and bind retain bounded backfill.                                                                                                                       |
| `c2dd4be3a0b8065fd67d8543` | valid+fixed      | Shared bounded-ID implementation added.                                                                                                                                                                                            |
| `b601aa745f1a54254496b9f4` | valid+fixed      | `uses trusted worker_done row lineage instead of forged payload fields` proves row lineage wins.                                                                                                                                   |
| `d3a2cd026bf9240b665106aa` | superseded-stale | `runCreate` persists a durable Run before best-effort controlled binding by design; wake is disabled-by-default and absence/failure of a pane binding cannot roll back Run creation. Run generation fencing handles later binding. |
| `c5d5ef472042106b44f4aa4f` | superseded-stale | `db.bindRun` atomically changes Run/generation state and fences old wake state; controlled-pane binding is intentionally post-commit and best-effort. Existing rebind/fencing tests cover this contract.                           |
| `53a1f9ae84bb7f1269db54cf` | valid+fixed      | Registry bulk disposal uses `Promise.allSettled` and throws one `AggregateError`.                                                                                                                                                  |
| `ce6bf19e0cf3370da62dce5c` | valid+fixed      | Session reconciliation uses `Promise.allSettled` across eligible sessions.                                                                                                                                                         |
| `a2112020cb293ef2ea2fcf63` | valid+fixed      | Background-surface and missing-pane terminal rejection tests prove cleanup.                                                                                                                                                        |
| `19388aac9ba250e10088b90a` | valid+fixed      | Fixture socket path is derived by `getControlledSocketPath`.                                                                                                                                                                       |
| `799fb1ebdae049ff25bf11f7` | valid+fixed      | Runtime binding test proves cleanup rejection cannot mask the original bind failure.                                                                                                                                               |
| `1b85432886554fa19b90bc42` | valid+fixed      | Smoke and tests use `tmpdir()` rather than hard-coded `/tmp` roots. Production default remains unchanged; the validated environment override supports build-flavor isolation.                                                      |
| `08c34f8a4dbbe650dc6a93f2` | valid+fixed      | Non-fresh smoke aggregates primary and cleanup errors instead of masking the primary failure.                                                                                                                                      |
| `33d427b7c39ac254b2c0d1c1` | valid+fixed      | Promise resolver renamed and signal termination covered by the shared smoke predicate.                                                                                                                                             |
| `e6c4e9909b6a89161e1e706a` | not-applicable   | Parser extraction is an optional refactor. Initial-turn and wake-turn parsers deliberately emit domain-specific response errors; changing that contract is not required for the confirmed defects.                                 |
| `f9c8098a0dd6439ba8592229` | not-applicable   | No safe retention rule exists in the v1 record schema: pruning finalized initial/wake records can violate idempotency. A future bounded-retention design needs durable age metadata and a compatibility migration.                 |
| `47903ab572496926680918e1` | already fixed    | SSH generation/timer fencing and regression tests already exist; no controlled-wake source change is needed.                                                                                                                       |

## Additional automated-tool notes

- The `ws://localhost/rpc` static warning is not applicable: `createConnection` replaces TCP with the owned private Unix-domain socket, so no localhost TCP traffic is used.
- The docstring-coverage and PR-description warnings are repository/PR policy notes, not runtime defects.
- The comment asking for a Windows ACL is satisfied by fail-closed platform admission: controlled sessions and their prompt-bearing state are POSIX-only in v1.
