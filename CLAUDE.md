@AGENTS.md

<!-- 포크 고유(Noobear/orca). upstream 머지 시 이 절만 유지한다. -->
## 이 포크의 자산·kw-repo 취급

`docs/assets/`·`resources/`·`mobile/assets/`의 바이너리는 kw-repo(`kw-repo/orca/`)에도
미러돼 있지만 **정본은 git 쪽이다** — kw-repo에 있다는 이유로 추적 해제하지 말 것.
경위·규칙: `KW-REPO.md` (3-tier 정본 = `orca-system` 레포 `STRUCTURE.md`).
