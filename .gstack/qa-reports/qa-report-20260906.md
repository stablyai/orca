# QA Report — feat/linux-serve-auto-update (2026-09-06)

Diff-aware mode. Branch surface: Linux serve auto-update (CLI command, main-process updater
handoff, root helper, docker E2E harness, i18n copy), renderer import refactor.

## Browser lane
- Live server on :6768 is the OLD installed build (v1.4.197) — cannot exercise branch code;
  restarting it would kill the user's live session (destructive — not attempted).
- Branch dev:web (vite :5188) + merge-base dev:web (:5199 from worktree at 0d2375a7ff2c):
  BOTH crash identically in unpaired mode (DocPreviewExternalLinkConfirmation,
  BrowserWebAuthnAccountDialog, WindowControls undefined-read render crashes).
  Verified pre-existing on merge-base — not branch-caused. Evidence: console logs captured,
  base-check note in this directory.

## Functional lanes (all green)
- src/cli suite: 111 files, 1053 passed.
- Updater/serve-update suite: 28 files, 301 passed.
- 11 renderer files touched by import refactor: 93 passed.
- Docker harness: run-update-case.sh bash -n OK; runner node --check OK;
  mock-feed.py compile + live HTTP smoke (both URL shapes: /latest/download/latest-linux.yml
  and /<tag>/latest-linux.yml) OK.

## Issues found and fixed during QA
1. mock-feed.py double-directory kwarg TypeError per request (regression from my earlier
   --root fix) — fixed via functools.partial(Handler, directory=args.root); HTTP smoke
   verifies both feed URL shapes; quiet logs preserved. Commit 0e41e912bf.
2. liveTerminals i18n key missing in fr/es/ko (fr = fully-translated catalog, fell back to
   English) — fr real translation added; es/ko English echo per locale policy. Commit fb80b6cc21.
3. (pre-existing, verified on merge-base) unpaired dev:web render crash — documented, not
   branch-caused, left as-is per fix-first scope rule (needs paired runtime to even repro the
   non-crashing state; crash exists identically at merge-base).

## Health
No branch-caused issues open. Deferred: none.
