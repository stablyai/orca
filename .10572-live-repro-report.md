# Issue #10572 live reproduction report

**Task:** `task_0313b845f28e`  
**Date:** 2026-07-25  
**Environment:** macOS, Orca production **1.4.156** (pid 50323, `com.stablyai.orca`)  
**Worktree:** `/Users/jinjingliang/Documents/projects/orca/agent-rows-stay-under-the-panes-original-worktre`  
**Mode:** diagnostic only — no tracked source edits  

**Result: REPRODUCED**

---

## Issue / PR independent read

### Issue #10572
Agent rows stay under the worktree whose tab the pane was created in, even after the pane’s shell/session has moved into a different worktree (classic flow: split pane → `claude -w`). Sidebar over-counts agents on the parent and does not surface the mismatch.

Root-cause claim in the issue (confirmed against this tree):
- `useWorktreeAgentRows` derives rows from `tabsByWorktree[worktreeId]` (pane→tab→worktree), not live cwd.
- `worktreeId` is fixed at PTY spawn; nothing re-attributes on cwd change.

### PR #10606 (treat as potentially wrong)
- **State:** open, unmerged into the running app / this worktree HEAD.
- **Approach:** keep pane-created attribution; when OSC 7 cwd maps to a **known sibling worktree** of the same repo, annotate compact secondary with `in <branch|name>`.
- **Does not:** move agent rows, fix the count model, or reveal agent-scratch worktrees.
- This worktree HEAD `468f5b77` does **not** contain PR commit `7a51cf20` (`merge-base --is-ancestor` → false).
- PR helper files (`live-pane-cwd-registry.ts`, `worktree-agent-live-worktree-mismatch.ts`) are **absent** from this checkout.

---

## Exact steps performed

1. Confirmed Orca ready: `orca status --json` → app running, version **1.4.156**.
2. Confirmed active worktree:  
   `48e366bb-…::…/agent-rows-stay-under-the-panes-original-worktre`  
   branch `agent-rows-stay-under-the-panes-original-worktre`, linked issue **10572**.
3. **Baseline** (`orca worktree ps --json` for this path):
   - `liveTerminalCount: 2`
   - agents: **codex** (coordinator) + **grok** (this worker) = **2**
4. Created shell tab (visible worktree tab):
   ```bash
   orca terminal create \
     --worktree path:…/agent-rows-stay-under-the-panes-original-worktre \
     --title "10572-repro-shell" --json
   ```
   → handle `term_9b0168f4-…`, tabId `f37a84be-…`
5. Split pane and launch Claude worktree flow (CLI equivalent of Cmd+D + `claude -w`):
   ```bash
   orca terminal split \
     --terminal term_9b0168f4-… \
     --direction horizontal \
     --command 'claude -w 10572-live-repro' --json
   ```
   → handle `term_2d9cacae-…`, same tabId `f37a84be-…`, leaf `8f9b5276-…`
6. Confirmed git worktree created:
   - Path: `/Users/jinjingliang/Documents/projects/orca/.claude/worktrees/10572-live-repro`
   - Branch: `worktree-10572-live-repro` @ `468f5b77` (same as parent; clean)
7. Sent minimal prompt into Claude pane:
   ```text
   Reply with only the word READY. Do not edit files, do not run tools.
   ```
8. Captured runtime + UI evidence (below).
9. **Cleanup:** closed both repro terminals; unlocked/removed the Claude git worktree; deleted branch `worktree-10572-live-repro`. Post-cleanup agents back to codex+grok (2).

---

## Observed vs expected

| Surface | Observed | Expected (per issue) |
|--------|----------|----------------------|
| Sidebar worktree card (parent) | Claude agent row listed under **parent** (“10572-repro-shell - READY”) | Either re-attribute to actual worktree, or make mismatch visible |
| Agent count under parent | `worktree ps` **3** agents (codex, grok, claude) while Claude’s real tree is sibling | Count should not silently include agents whose work is elsewhere without indication |
| Sibling `.claude/worktrees/10572-live-repro` in Orca sidebar | **Not registered** as an Orca worktree (`worktree list` matches for `.claude/worktrees`: **0**) | Once agent-scratch rows exist (#10560), would show idle while parent over-counts; today worktree is hidden *and* agent mis-filed |
| Claude session path in TUI | `~/Documents/projects/orca/.claude/worktrees/10572-live-repro` | Same |
| Compact secondary annotation `in worktree-…` | **Absent** on production 1.4.156 | Only if PR #10606 (or similar) shipped |

---

## Concrete runtime / process / store evidence

### Process cwd (mis-match vs Orca attribution)
```
PID 26413  claude -w 10572-live-repro
  cwd = /Users/jinjingliang/Documents/projects/orca/.claude/worktrees/10572-live-repro

PID 26196  bash (parent of claude, Orca shell-ready rcfile)
  cwd = /Users/jinjingliang/Documents/projects/orca/agent-rows-stay-under-the-panes-original-worktre
```
Claude’s **process** cwd is the sibling worktree; the spawning shell stayed on the parent path.

### Terminal handle metadata (still parent)
```json
{
  "handle": "term_2d9cacae-53ee-442e-a600-f45e36390fa0",
  "title": "✳ Reply with READY only",
  "worktreeId": "48e366bb-…::…/agent-rows-stay-under-the-panes-original-worktre",
  "worktreePath": "…/agent-rows-stay-under-the-panes-original-worktre",
  "branch": "refs/heads/agent-rows-stay-under-the-panes-original-worktre",
  "tabId": "f37a84be-56c8-4465-8414-bc1eb03ff55d",
  "leafId": "8f9b5276-01aa-45d5-87ca-19eb82bb04b9",
  "ptyId": "48e366bb-…::…/agent-rows-stay-under-the-panes-original-worktre@@d79f158a"
}
```
`paneKey` for the mis-attributed agent:  
`f37a84be-56c8-4465-8414-bc1eb03ff55d:8f9b5276-01aa-45d5-87ca-19eb82bb04b9`

### `orca worktree ps` (parent card)
During repro:
- path: `…/agent-rows-stay-under-the-panes-original-worktre`
- `liveTerminalCount: 4`, **agentCount: 3**
  - codex working (coordinator pane)
  - grok working/done (this worker)
  - **claude done** — prompt “Reply with only the word READY…” on the split leaf above

No Orca worktree entry for `10572-live-repro` / any `.claude/worktrees/*`.

### UI screenshot
`.10572-repro-ui.png` (sidebar shows parent card expanded with Claude row under it; Claude TUI header path is the `.claude/worktrees/10572-live-repro` directory).

### Code model in this tree (unchanged, matches issue)
`src/renderer/src/components/sidebar/useWorktreeAgentRows.ts` still binds via:
```ts
const tabs = useAppStore((s) => (active ? s.tabsByWorktree[worktreeId] : undefined))
```
No live-cwd mismatch helpers present on this branch.

---

## Caveats

1. **App under test is production 1.4.156**, not a build of PR #10606. Reproduction validates the *current* product behavior, not the PR’s effectiveness.
2. Split was done via `orca terminal split` (not keyboard Cmd+D). Same tab/pane topology: split leaf inherits parent tab → same `tabsByWorktree` binding.
3. **Shell vs process cwd:** parent bash cwd remained on the original worktree while Claude’s process cwd was the sibling. Any fix that relies solely on shell OSC 7 may miss this if OSC tracks the shell and not the agent child. Worth verifying before trusting PR #10606’s OSC-only path for `claude -w` specifically.
4. Temporary worktree was **clean** (no commits/diffs beyond checkout of same HEAD) and was **removed** after the test. Pre-existing unrelated `.claude/worktrees/agent-*` entries were left untouched.
5. UI accessibility tree for Electron Orca is nearly empty; visual evidence is from screenshot + CLI `worktree ps` / `terminal show` / `lsof` cwd.

---

## Implications for PR #10606 (review stance)

- The live bug is real; the issue’s model diagnosis is accurate on 1.4.156.
- PR #10606’s chosen fix (annotate, don’t re-attribute) **does not** address over-count or “agent under wrong worktree card” structurally — it only hopes to make the mismatch legible when OSC 7 maps to a known sibling.
- Whether that annotation would have fired in this exact repro is **unproven** here (PR not running; shell OSC may still be parent). Recommend a second pass on a build of #10606 with the same `claude -w` steps before merging as “closes #10572”.
- Agent-scratch hide policy + pane-created attribution still combine into a contradictory UI story (comment on the issue by jeongph remains valid).

---

## Cleanup status

| Artifact | Status |
|----------|--------|
| Terminals `10572-repro-shell` / Claude split | Closed; PTYs killed |
| Git worktree `.claude/worktrees/10572-live-repro` | Removed |
| Branch `worktree-10572-live-repro` | Deleted |
| Parent worktree agents | Restored to codex + grok only |
| Tracked files | **Unmodified** |
| Untracked diagnostic assets | `.10572-live-repro-report.md`, `.10572-repro-ui.png` |
