/**
 * Stage, Priority and Assignees share one fixed width so the header row keeps
 * the same geometry from ticket to ticket instead of reflowing per value.
 */
export const ODOO_TICKET_CONTROL_WIDTH_CLASS = 'w-40'
