import type { SettingsSearchEntry } from './settings-search'

export const DOCKER_IMAGES_PANE_SEARCH_ENTRIES: SettingsSearchEntry[] = [
  {
    title: 'Docker Images',
    description: 'Cached Docker images used by isolated worktrees.',
    keywords: ['docker', 'images', 'cache', 'containers', 'prune', 'disk']
  }
]
