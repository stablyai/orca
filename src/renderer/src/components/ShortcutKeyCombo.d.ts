import React from 'react';
type ShortcutKeyComboProps = {
    keys: string[];
    separatorClassName?: string;
};
export declare function ShortcutKeyCombo({ keys, separatorClassName }: ShortcutKeyComboProps): React.JSX.Element;
export {};
