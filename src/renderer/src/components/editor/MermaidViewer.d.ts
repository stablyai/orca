import React from 'react';
type MermaidViewerProps = {
    content: string;
    filePath: string;
};
export default function MermaidViewer({ content, filePath }: MermaidViewerProps): React.JSX.Element;
export {};
