import React from 'react';
export declare function LazySection({ index, onVisible, children }: {
    index: number;
    onVisible: (index: number) => void;
    children: React.ReactNode;
}): React.JSX.Element;
