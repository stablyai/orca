import type { BuiltInWindowsTerminalShell } from '../../../../shared/windows-terminal-shell'

export function resolveWindowsShellLaunchTarget(
  shell: BuiltInWindowsTerminalShell
): BuiltInWindowsTerminalShell {
  return shell
}
