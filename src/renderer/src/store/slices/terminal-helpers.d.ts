import type { TerminalLayoutSnapshot, TerminalTab } from '../../../../shared/types';
export declare function emptyLayoutSnapshot(): TerminalLayoutSnapshot;
export declare function clearTransientTerminalState(tab: TerminalTab, index: number): TerminalTab;
