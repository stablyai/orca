name: Unit baseline measurement
run-name: Unit baseline on shard candidate source
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  test:
    uses: ./.github/workflows/unit-tests.yml
    with:
      node_versions: '["24"]'
